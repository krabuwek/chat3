<%= form_for [@room, Message.new] do |f| %>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text, cols: 40 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>